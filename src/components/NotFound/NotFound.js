import React from 'react';
import './NotFound.css';
const NotFound = () => {
    return (
        <div>
            <h1>Sorry we can't navigate.</h1>
        </div>
    );
};

export default NotFound;